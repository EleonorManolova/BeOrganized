@using BeOrganized.Common;
@model IEnumerable<BeOrganized.Data.Models.Calendar>
<section class="content-header">
    <h1>
        Calendars
    </h1>
    <ol class="breadcrumb">
        <li><a asp-area="Administration" asp-controller="Dashboard" asp-action="Index"><i class="fa fa-dashboard"></i> Home</a></li>
        <li class="active">Calendars</li>
    </ol>
</section>
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Manage Calendars</h3>
                </div>
                <div class="box-body">
                    <table id="table" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Title</th>
                                <th>UserId</th>
                                <th>Created On</th>
                                <th>Events Count</th>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>@item.Title</td>
                                    <td>@item.UserId</td>
                                    <td>@item.CreatedOn</td>
                                    <td><a asp-area="Administration" asp-controller="Calendars" asp-action="Edit" asp-route-calendarId="@item.Id">Edit</a> | <a asp-area="Administration" asp-controller="Calendars" asp-action="Delete" asp-route-calendarId="@item.Id">Delete</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
